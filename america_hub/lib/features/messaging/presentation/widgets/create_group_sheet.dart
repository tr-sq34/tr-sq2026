import 'package:flutter/material.dart';
import '../../../../core/constants/app_colors.dart';
import '../../application/messaging_controller.dart';
import '../../domain/entities/conversation.dart';

class CreateGroupSheet extends StatefulWidget { const CreateGroupSheet({super.key,required this.controller}); final MessagingController controller; @override State<CreateGroupSheet> createState()=>_CreateGroupSheetState(); }
class _CreateGroupSheetState extends State<CreateGroupSheet>{final _name=TextEditingController();final _city=TextEditingController(text:'New York, NY');GroupPrivacy _privacy=GroupPrivacy.public;@override void dispose(){_name.dispose();_city.dispose();super.dispose();}Future<void> _create()async{final ok=await widget.controller.createGroup(name:_name.text,city:_city.text,privacy:_privacy);if(!mounted)return;if(ok)Navigator.pop(context);else ScaffoldMessenger.of(context).showSnackBar(SnackBar(content:Text(widget.controller.errorMessage??'Grup adı en az 3 karakter olmalı ve şehir bilgisi girilmeli.')));}@override Widget build(BuildContext context)=>Padding(padding:EdgeInsets.only(bottom:MediaQuery.viewInsetsOf(context).bottom),child:Material(color:Colors.white,borderRadius:const BorderRadius.vertical(top:Radius.circular(28)),child:SafeArea(top:false,child:Padding(padding:const EdgeInsets.all(20),child:Column(mainAxisSize:MainAxisSize.min,crossAxisAlignment:CrossAxisAlignment.start,children:[const Text('Yeni grup oluştur',style:TextStyle(fontSize:21,fontWeight:FontWeight.w800)),const SizedBox(height:16),CircleAvatar(radius:27,backgroundColor:const Color(0xFFF0E9FF),child:IconButton(onPressed:(){},icon:const Icon(Icons.add_a_photo_outlined,color:AppColors.primary))),const SizedBox(height:12),TextField(controller:_name,decoration:InputDecoration(labelText:'Grup adı',prefixIcon:const Icon(Icons.groups_outlined,color:AppColors.primary))),const SizedBox(height:12),TextField(controller:_city,decoration:InputDecoration(labelText:'Şehir / eyalet',prefixIcon:const Icon(Icons.location_on_outlined,color:AppColors.primary))),const SizedBox(height:16),SegmentedButton<GroupPrivacy>(segments:const[ButtonSegment(value:GroupPrivacy.public,label:Text('Açık'),icon:Icon(Icons.public)),ButtonSegment(value:GroupPrivacy.private,label:Text('Gizli'),icon:Icon(Icons.lock_outline))],selected:{_privacy},onSelectionChanged:(value)=>setState(()=>_privacy=value.first)),const SizedBox(height:18),SizedBox(width:double.infinity,child:FilledButton.icon(onPressed:_create,icon:const Icon(Icons.add),label:const Text('Grubu oluştur')))])))));}
