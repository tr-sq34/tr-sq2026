# Keep the Gradle daemon within a practical desktop-memory budget. Higher values
# can fail before a build starts when Windows cannot reserve paging-file space.
org.gradle.jvmargs=-Xmx1536m -XX:MaxMetaspaceSize=512m -XX:ReservedCodeCacheSize=256m -Dfile.encoding=UTF-8
android.useAndroidX=true
